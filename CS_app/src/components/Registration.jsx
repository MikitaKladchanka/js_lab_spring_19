import React from 'react';
import r from './Registration.module.css';

const Registration = (props) => {
    return (
		<div className={r.regWrap}>
			Registration
		</div>
    )
}

export default Registration;